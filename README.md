# Building JL Extension with Webpack
This is an example of how to pre-build a JL extension with webpack, so that
you can run it through whatever config you want.


```bash
yarn
yarn build
jupyter labextension install .
```